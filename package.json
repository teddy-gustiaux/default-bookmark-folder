{
  "name": "default-bookmark-folder",
  "version": "2.4.1",
  "description": "WebExtension allowing you to choose the default bookmark location folder and quickly bookmark pages there via a dedicated icon.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "eslint": "eslint ",
    "lint": "web-ext lint --source-dir ./source/",
    "start:firefox": "web-ext run --source-dir ./source/ --firefox=firefox --firefox-profile=dev --start-url about:debugging",
    "start:nightly": "web-ext run --source-dir ./source/ --firefox=nightly --firefox-profile=nightly --start-url about:debugging",
    "start:dev": "web-ext run --source-dir ./source/ --firefox=firefoxdeveloperedition --firefox-profile=dev --start-url about:debugging",
    "start:waterfox": "web-ext run --source-dir ./source/ --pre-install --firefox=\"C:\\Program Files\\Waterfox\\Waterfox.exe\" --start-url about:debugging",
    "build": " web-ext build --source-dir ./source/ --overwrite-dest --artifacts-dir ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teddy-gustiaux/default-bookmark-folder.git"
  },
  "keywords": [
    "webextension",
    "javascript",
    "firefox",
    "browser-extension",
    "firefox-extension",
    "bookmarks"
  ],
  "author": "Teddy Gustiaux",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/teddy-gustiaux/default-bookmark-folder/issues"
  },
  "homepage": "https://github.com/teddy-gustiaux/default-bookmark-folder#readme",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.12.0",
    "eslint-plugin-node": "^6.0.1",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0",
    "web-ext": "^2.7.0"
  }
}
